# Chill Bros server-only configuration. Never commit real values for the
# service role key below — this file only documents variable names.

# Supabase project (shared with bb-owner-command-center's Supabase project,
# isolated by the chillbros_ table prefix). The URL and the anon/publishable
# key are not secret and are safe to check in as-is; the service role key
# below is a real secret and must only ever be set in Vercel's Environment
# Variables, never here or in chat.
SUPABASE_URL=https://xespxlqcjvhompsxranc.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inhlc3B4bHFjanZob21wc3hyYW5jIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODc0MDM1MTcsImV4cCI6MjEwMjk3OTUxN30.3aq-z1g5e8Yr3Uzmw2RuRZW9Q46_mdm49KqcAOT8kvA
SUPABASE_SERVICE_ROLE_KEY=

# Stripe (card / Apple Pay charging). Cash App, Venmo, and Zelle stay
# manual-confirm since none of them offer a business API. Real secret —
# Vercel env vars only.
STRIPE_SECRET_KEY=

# Gmail SMTP for outbound quote/approval/receipt email (chillbrostx@gmail.com).
# Use a Gmail App Password (Google Account > Security > 2-Step Verification >
# App Passwords), never the account's normal login password. Real secret —
# Vercel env vars only.
GMAIL_SMTP_USER=chillbrostx@gmail.com
GMAIL_SMTP_APP_PASSWORD=
