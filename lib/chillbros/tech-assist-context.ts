import "server-only";
import { createServiceRoleClient } from "@/lib/supabase/service-client";
export async function getTechAssistContext(jobId:string){const s=createServiceRoleClient();const {data:job}=await s.from("chillbros_jobs").select("id,job_number,status,scope,work_performed,location,scheduled_window,equipment_id,customer:chillbros_customers(name),equipment:chillbros_equipment(asset_tag,equipment_type,manufacturer,model,serial_number,refrigerant,notes)").eq("id",jobId).maybeSingle();if(!job)return null;const [{data:events},{data:parts},{data:history}]=await Promise.all([s.from("chillbros_workflow_events").select("stage,message,created_at").eq("job_id",jobId).order("created_at",{ascending:false}).limit(30),s.from("chillbros_job_parts").select("quantity,part:chillbros_parts_catalog(name,part_number)").eq("job_id",jobId),job.equipment_id?s.from("chillbros_jobs").select("job_number,status,scope,work_performed,created_at").eq("equipment_id",job.equipment_id).neq("id",jobId).order("created_at",{ascending:false}).limit(15):Promise.resolve({data:[]})]);return {job,events:events??[],parts:parts??[],equipmentHistory:history??[]};}