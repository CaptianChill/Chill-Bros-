CORRECTED 2026-09-21: this file previously said cutover was complete. It was not, and saying so caused real damage — a day's worth of fixes got applied to Neon while production kept running on the original Supabase project (`bbb-sports-intelligence`, id `xespxlqcjvhompsxranc`). A migration to Neon was attempted at some point and reverted (see git history: "Revert 'Enforce Neon as the single Chill Bros backend'"). Neon Auth may still be live for sign-in independent of this. See `docs/database-source-of-truth.md` for the full, re-verified picture, the proof method that actually works (a write test, not a data read), and the list of fixes made to the wrong database that still need redoing against the real one.